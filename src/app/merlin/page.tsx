import { MerlinTerminal } from "@/components/merlin/MerlinTerminal";

export const metadata = {
  title: "Merlin — Portfolio Builder | Vaulty",
  description: "AI-powered alternative investment portfolio optimizer. Get a custom allocation with stress tests and forecasts in 30 seconds.",
};

export default function MerlinPage() {
  return <MerlinTerminal />;
}
